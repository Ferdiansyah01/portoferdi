/**
 * generateAssets.js
 * Run once with: node src/utils/generateAssets.mjs
 * Creates placeholder PNG spritesheets and tilesets in public/assets/
 * using the Node.js 'canvas' package (or skip — Phaser can use
 * programmatic textures at runtime via this.textures.generate()).
 *
 * NOTE: This file is documentation only.
 * The actual sprites are generated at Phaser RUNTIME inside BootScene.js
 * using Phaser's built-in texture generation API — no PNG files required.
 */
